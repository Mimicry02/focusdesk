// All data requests use same-origin HttpOnly cookies. No secrets or session tokens in localStorage.
export async function api(path,options={}){const r=await fetch('/api/'+path,{credentials:'same-origin',cache:'no-store',...options,headers:{'Content-Type':'application/json',...options.headers},body:options.body===undefined?undefined:JSON.stringify(options.body)});let data;try{data=await r.json()}catch{throw Error('API tidak tersedia. Deploy seluruh proyek termasuk folder api/, bukan hanya public/.')}if(!r.ok){const e=Error(data.error||'Permintaan gagal');e.status=r.status;throw e;}return data;}
